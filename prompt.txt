The Verilog RTL module for the microcode sequencer located at source/instruction_decoder_6.v is failing simulation. 
The simulation fails at the test which sends opcode 13. There are more tests in the testcase which are untested as the failure occurs before all the tests could be run.

Debug and fix the RTL file based on the specification which is available at docs/Specification.md. Ensure all issues are resolved so that the RTL is able to pass the testbench.